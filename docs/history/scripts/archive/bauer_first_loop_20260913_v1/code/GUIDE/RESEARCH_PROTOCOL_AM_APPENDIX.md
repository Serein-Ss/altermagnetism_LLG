# 交错磁模型、热扩展与资源附录

版本：2026-09-13，配套主协议修订4。[返回主协议](PUBLICATION_GRADE_RESEARCH_PROTOCOL_20260911.md#am)；[参考文献](RESEARCH_PROTOCOL_REFERENCES.md)。

本附录只在主协议P6启用，当前不冻结AM生产矩阵。原条件复现、均匀热扩展与有驱动复现使用不同protocol_id；经典链GO不替代AM认证。

## 1. Hamiltonian与晶胞

Gomonay幅值为`J1=11.1,J2=1.88,J_tilde=0.8 meV`，取 `E0=11.1 meV`。当前100晶胞的跨子晶格四条键为 `-J1/E0`，同子晶格轴向键为 `+J2/E0`；子晶格符号sigma为+1/-1，对角 `(1,-1)` 权重 `-sigma*J_tilde/E0`，`(1,1)` 为 `+sigma*J_tilde/E0`。110通过精确超晶胞映射产生四基点，不能复制两基点索引冒充同一几何。独立审计以原文补充材料为准，代码内部一致性不是外部认证。

## 2. Gilbert随机方程与温度模型

对相同磁矩和正旋磁比，使用主协议的通用约化尺度。`equation_convention=gilbert,damping_parameter=alpha`，采用Stratonovich解释；不可将本式替换Bauer的特殊LL。

```math
\begin{aligned}
d\mathbf S_i={}&-\frac{1}{1+\alpha^2}\Bigl(\\
&\mathbf S_i\times[\mathbf b_i d\tilde t+\sqrt{2\alpha\vartheta}\circ d\mathbf W_i]\\
&+\alpha\mathbf S_i\times\{\mathbf S_i\times[\mathbf b_i d\tilde t+\sqrt{2\alpha\vartheta}\circ d\mathbf W_i]\}\Bigr),\\
E[dW_{i\mu}dW_{j\nu}]&=\delta_{ij}\delta_{\mu\nu}d\tilde t.
\end{aligned}
```

积分器分工、步长/保存收敛和噪声耦合遵循主协议2.3。

`K_SW=0`仅用于规定的零温色散基准；有限温零场路径采用 `K=K_DW=0.047 meV` 易轴模型作为待认证扩展，配置显式记录 `thermal_model_id,K/E0`，不依赖wall参数默认值。K=0研究作为不同模型另行登记。

二维、有限层数、短程且自旋旋转连续对称的K=0模型不预设非零热力学T_N；空间交换各向异性不自动解除该限制[MerminWagner1966]。先导温度用 `theta=kBT/E0`，不输入未经确定的T/T_N。有限尺寸交叉温度记为T_star(L)，与热力学T_N分开。

相对温度需独立校准：多尺寸平衡、Binder/磁化率和尺寸标度，冻结估计与不确定性。模型T_N不等于块体RuO2实验温度。不得用最终测试动力学结果选择温度标尺。改变K、边界或模型后重新检查。接近/高于临界区仅对所定义的经典固定模长模型作结论，不自动外推为真实材料定量预测。

## 3. 无阻尼色散基准

本节明确采用文献幅值：`j1=J1/E0>0,j2=J2/E0>0,jt=J_tilde/E0,kappa=K/E0`；它们不是邻接表中的有符号键权，尤其不能将跨子晶格的负键权代入j1。kx/ky以a0约化且沿原始晶轴：

```math
\begin{aligned}
a&=\cos(k_x/2)\cos(k_y/2),\\
b&=1+\kappa/(2j_1)+(j_2/j_1)[\sin^2(k_x/2)+\sin^2(k_y/2)],\\
c&=(j_t/j_1)\sin k_x\sin k_y.
\end{aligned}
```

```math
\begin{aligned}
\tilde\omega_\pm&=4j_1(\sqrt{b^2-a^2}\pm c),\\
\omega_\pm&=\tilde\omega_\pm/t_0,\\
f_\pm&=\omega_\pm/(2\pi).
\end{aligned}
```

这是对应无阻尼线性基准；110倒空间和带折叠映射到同一物理波矢。磁矩/旋磁比未审定前只报约化频率。

## 4. 文献对照和热路径放行

当前Gomonay型模型以[Gomonay2024]为主锚点：先复现Fig.2磁振子分裂、Fig.3畴壁Néel/局部磁化剖面及方向依赖；研究运动畴壁时再对照Fig.5及SI的速度、形变和进动结果。对应参数、晶向、几何、边界、初态、阻尼与单位逐项匹配；主文/SI图号差异按版本登记。零交错交换控制和能量梯度自检不能替代外部曲线。Fig.4磁尖端外场属于独立驱动复现，不隐含加入零场训练。

[Weissenhofer2024]提供直接有限温随机LLG参考：附录C的高斯白噪声/FDT、Heun方法，Fig.10的序参量—温度关系，以及温度梯度下自旋积累和输运对方向、温度与阻尼的依赖。该论文使用自己的三维交换模型和热浴条件，不是当前二维Gomonay简化模型的同参数数值真值。若采用其模型，必须按原文对照；若保持Gomonay模型，只引用方法依据，不以曲线数值强配。原文为稳态而设定的磁矩/旋磁比单位不能直接当真实皮秒标尺。

新增均匀温度反转路径属于热扩展，不是以上论文已公布的路径分布。本次未核实到匹配当前模型的公开固定初态多噪声完整轨迹包。先认证所选模型原文覆盖的结果，再检查热扩展的平衡、步长、噪声和事件统计；保存所有自旋路径及未反转样本。论文中的均值、谱或稳态输运正确不能单独证明联合路径分布正确。AM合同冻结前禁止批量生产；任何缩尺寸、改K、改边界或热浴都登记为扩展并重新评估适用范围。

AM合同另行定义数据矩阵、事件与无事件机制、适用对称操作、物理指标/容差、独立留出和预算。C4必须结合实际晶胞、子晶格和位移映射检查；非正方形或开放边界不预设固定几何内的C4不变性。100/110比较须匹配物理盒长、边界和观测方向，纯表示变换不作为晶向效应。

## 5. 历史资源算例：非执行矩阵

100的L16/32/64/128和110的L16/32/64，共7几何，8温度、3阻尼、3初态类型、每类8初态、每初态32噪声，总129024。8个T/T_N只有独立标尺完成后可用，否则登记8个theta。

按100/110分别2/4自旋、1001帧、三分量float32，未压缩spins约14.40TB（十进制）。逐帧三分量field另需近似同量，尚有备份、统计和生成结果。压缩比实测；盒长为匹配物理尺寸而变化时重新计算。

算式采用每几何18432条路径：`8*3*3*8*32=18432`；总数`7*18432=129024`。未压缩自旋字节数为：

```text
18432 * 1001 * 3 * 4 *
[2*(16^2+32^2+64^2+128^2) + 4*(16^2+32^2+64^2)]
= 14,396,650,684,416 bytes = 14.396650684416 TB
```

这是旧假设下的算术复核，不是当前任务的预算或实测压缩率。匹配100/110物理盒长、改变帧数、增加参考集合或测试集后须重新计算；正式AM预算遵循主协议6.2。
