# 当前交错磁 sLLG 与 `example/微磁学.txt` 的差异

## 结论

当前代码不是把旧 C 程序换成 PyTorch 语法，而是保留“周期晶格上的 LLG”这一
共同骨架后，更换成文献约束的双子晶格交错磁自旋晶格模型，并修复旧程序不适合
生成统计轨迹数据的数值流程。因此，两者不能逐点比较轨迹，也不能把旧程序的
无量纲参数直接换算成 RuO2 参数。

| 层次 | `example/微磁学.txt` | 当前 `altermagnetism_LLG` |
|---|---|---|
| 物理对象 | 单层 `8 x 8`、每格点一个自旋 | 两个错开半个对角晶格矢量的磁性子晶格 |
| Hamiltonian | 最近邻交换、DMI、单离子各向异性、外场 | 文献 Eq. (S.1)：层间 AFM `J1`、层内 FM `J2`、交错对角交换 `J_tilde`、易轴各向异性 |
| 参数 | `Js=-1, D=0.5, Kz=0.05`，无量纲 | `J1=11.1, J2=1.88, J_tilde=0.8, K=0.047 meV`，并使用 `mu_B`、tesla 和 second |
| 自旋表示 | 球坐标 `theta, phi` | 笛卡尔单位向量 `(sx, sy, sz)`，每步投影到 `S2` |
| 确定性积分 | 名义 RK4，但每个格点四个 stage 共用一次有效场；格点原位顺序更新 | 同步全晶格 RK4；每个 stage 重新计算整个晶格有效场 |
| 有限温 | 没有热噪声；程序是确定性的 | 涨落耗散热场，Stratonovich 随机 Heun，同一噪声用于 predictor/corrector |
| 驱动 | 代码含 STT 形式，但默认 `J=0` | 显式有限时长 damping-like / field-like SOT pulse |
| 周期边界 | `x,y` 周期边界 | `x,y` 周期边界，并测试平移不变性和 C4/子晶格联合对称性 |
| 更新独立性 | 后更新格点能看到本 sweep 已更新邻居 | 所有格点同步更新；batch 内轨迹互不耦合 |
| 输出 | `1.txt` 到 `11.txt`；一个格点的时间序列和末态 | 每条轨迹完整快照、Néel 矢量、磁化、能量、种子、协议和等权元数据 |
| 研究用途 | 单条无量纲原型轨迹 | 文献复现、有限温路径统计和未来条件生成模型训练 |

## 直接代码证据

- 旧程序第 6-7 行固定 `8 x 8`；第 49-62 行固定全部无量纲参数。
- 旧程序第 96-98 行只在每个格点更新前计算一次局域场，第 99-106 行的四个
  RK stage 复用该场，第 107-108 行立即原位写回。
- 旧程序第 114-118 行只记录 `(0,0)` 自旋和能量；第 120-128 行只记录一次
  末态，没有轨迹集合、随机种子或温度定义。
- 当前 Hamiltonian 的有效场已与自动微分的 `-dH/(mu_s ds)` 对照，且通过
  周期平移与 C4/子晶格联合对称性测试。

## 不能声称的等价关系

旧代码的 `D=0.5` 是普通 DMI，当前的 `J_tilde=0.8 meV` 是随子晶格和对角线
改变符号的各向异性交换；二者不是同一个物理项。旧代码也没有两个反平行子晶格，
所以不能通过简单修改 `Js`、`D` 和 `Kz` 得到当前交错磁模型。
